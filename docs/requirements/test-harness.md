# Test Harness Baseline

- `TEST-UI-001`: macOS UI test target can launch the app successfully.
- `TEST-UI-002`: macOS UI test target captures launch performance metrics.
- `TEST-UI-003`: macOS launch test captures a launch screenshot artifact.
